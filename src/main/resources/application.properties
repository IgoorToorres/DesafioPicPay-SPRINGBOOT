spring.application.name=desafioPicPay

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/picpay}
spring.datasource.username=${DB_USERNAME:picpay}
spring.datasource.password=${DB_PASSWORD:picpay_dev_password}

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.open-in-view=false
